"use strict";
(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __esm = (fn, res) => function __init() {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  };
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));

  // node_modules/.pnpm/base64-js@1.5.1/node_modules/base64-js/index.js
  var require_base64_js = __commonJS({
    "node_modules/.pnpm/base64-js@1.5.1/node_modules/base64-js/index.js"(exports) {
      "use strict";
      init_dist();
      exports.byteLength = byteLength;
      exports.toByteArray = toByteArray;
      exports.fromByteArray = fromByteArray;
      var lookup = [];
      var revLookup = [];
      var Arr = typeof Uint8Array !== "undefined" ? Uint8Array : Array;
      var code = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
      for (i = 0, len = code.length; i < len; ++i) {
        lookup[i] = code[i];
        revLookup[code.charCodeAt(i)] = i;
      }
      var i;
      var len;
      revLookup["-".charCodeAt(0)] = 62;
      revLookup["_".charCodeAt(0)] = 63;
      function getLens(b64) {
        var len2 = b64.length;
        if (len2 % 4 > 0) {
          throw new Error("Invalid string. Length must be a multiple of 4");
        }
        var validLen = b64.indexOf("=");
        if (validLen === -1)
          validLen = len2;
        var placeHoldersLen = validLen === len2 ? 0 : 4 - validLen % 4;
        return [validLen, placeHoldersLen];
      }
      function byteLength(b64) {
        var lens = getLens(b64);
        var validLen = lens[0];
        var placeHoldersLen = lens[1];
        return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
      }
      function _byteLength(b64, validLen, placeHoldersLen) {
        return (validLen + placeHoldersLen) * 3 / 4 - placeHoldersLen;
      }
      function toByteArray(b64) {
        var tmp;
        var lens = getLens(b64);
        var validLen = lens[0];
        var placeHoldersLen = lens[1];
        var arr = new Arr(_byteLength(b64, validLen, placeHoldersLen));
        var curByte = 0;
        var len2 = placeHoldersLen > 0 ? validLen - 4 : validLen;
        var i2;
        for (i2 = 0; i2 < len2; i2 += 4) {
          tmp = revLookup[b64.charCodeAt(i2)] << 18 | revLookup[b64.charCodeAt(i2 + 1)] << 12 | revLookup[b64.charCodeAt(i2 + 2)] << 6 | revLookup[b64.charCodeAt(i2 + 3)];
          arr[curByte++] = tmp >> 16 & 255;
          arr[curByte++] = tmp >> 8 & 255;
          arr[curByte++] = tmp & 255;
        }
        if (placeHoldersLen === 2) {
          tmp = revLookup[b64.charCodeAt(i2)] << 2 | revLookup[b64.charCodeAt(i2 + 1)] >> 4;
          arr[curByte++] = tmp & 255;
        }
        if (placeHoldersLen === 1) {
          tmp = revLookup[b64.charCodeAt(i2)] << 10 | revLookup[b64.charCodeAt(i2 + 1)] << 4 | revLookup[b64.charCodeAt(i2 + 2)] >> 2;
          arr[curByte++] = tmp >> 8 & 255;
          arr[curByte++] = tmp & 255;
        }
        return arr;
      }
      function tripletToBase64(num) {
        return lookup[num >> 18 & 63] + lookup[num >> 12 & 63] + lookup[num >> 6 & 63] + lookup[num & 63];
      }
      function encodeChunk(uint8, start, end) {
        var tmp;
        var output = [];
        for (var i2 = start; i2 < end; i2 += 3) {
          tmp = (uint8[i2] << 16 & 16711680) + (uint8[i2 + 1] << 8 & 65280) + (uint8[i2 + 2] & 255);
          output.push(tripletToBase64(tmp));
        }
        return output.join("");
      }
      function fromByteArray(uint8) {
        var tmp;
        var len2 = uint8.length;
        var extraBytes = len2 % 3;
        var parts = [];
        var maxChunkLength = 16383;
        for (var i2 = 0, len22 = len2 - extraBytes; i2 < len22; i2 += maxChunkLength) {
          parts.push(encodeChunk(uint8, i2, i2 + maxChunkLength > len22 ? len22 : i2 + maxChunkLength));
        }
        if (extraBytes === 1) {
          tmp = uint8[len2 - 1];
          parts.push(
            lookup[tmp >> 2] + lookup[tmp << 4 & 63] + "=="
          );
        } else if (extraBytes === 2) {
          tmp = (uint8[len2 - 2] << 8) + uint8[len2 - 1];
          parts.push(
            lookup[tmp >> 10] + lookup[tmp >> 4 & 63] + lookup[tmp << 2 & 63] + "="
          );
        }
        return parts.join("");
      }
    }
  });

  // node_modules/.pnpm/ieee754@1.2.1/node_modules/ieee754/index.js
  var require_ieee754 = __commonJS({
    "node_modules/.pnpm/ieee754@1.2.1/node_modules/ieee754/index.js"(exports) {
      init_dist();
      exports.read = function(buffer, offset, isLE, mLen, nBytes) {
        var e, m;
        var eLen = nBytes * 8 - mLen - 1;
        var eMax = (1 << eLen) - 1;
        var eBias = eMax >> 1;
        var nBits = -7;
        var i = isLE ? nBytes - 1 : 0;
        var d = isLE ? -1 : 1;
        var s2 = buffer[offset + i];
        i += d;
        e = s2 & (1 << -nBits) - 1;
        s2 >>= -nBits;
        nBits += eLen;
        for (; nBits > 0; e = e * 256 + buffer[offset + i], i += d, nBits -= 8) {
        }
        m = e & (1 << -nBits) - 1;
        e >>= -nBits;
        nBits += mLen;
        for (; nBits > 0; m = m * 256 + buffer[offset + i], i += d, nBits -= 8) {
        }
        if (e === 0) {
          e = 1 - eBias;
        } else if (e === eMax) {
          return m ? NaN : (s2 ? -1 : 1) * Infinity;
        } else {
          m = m + Math.pow(2, mLen);
          e = e - eBias;
        }
        return (s2 ? -1 : 1) * m * Math.pow(2, e - mLen);
      };
      exports.write = function(buffer, value, offset, isLE, mLen, nBytes) {
        var e, m, c;
        var eLen = nBytes * 8 - mLen - 1;
        var eMax = (1 << eLen) - 1;
        var eBias = eMax >> 1;
        var rt = mLen === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0;
        var i = isLE ? 0 : nBytes - 1;
        var d = isLE ? 1 : -1;
        var s2 = value < 0 || value === 0 && 1 / value < 0 ? 1 : 0;
        value = Math.abs(value);
        if (isNaN(value) || value === Infinity) {
          m = isNaN(value) ? 1 : 0;
          e = eMax;
        } else {
          e = Math.floor(Math.log(value) / Math.LN2);
          if (value * (c = Math.pow(2, -e)) < 1) {
            e--;
            c *= 2;
          }
          if (e + eBias >= 1) {
            value += rt / c;
          } else {
            value += rt * Math.pow(2, 1 - eBias);
          }
          if (value * c >= 2) {
            e++;
            c /= 2;
          }
          if (e + eBias >= eMax) {
            m = 0;
            e = eMax;
          } else if (e + eBias >= 1) {
            m = (value * c - 1) * Math.pow(2, mLen);
            e = e + eBias;
          } else {
            m = value * Math.pow(2, eBias - 1) * Math.pow(2, mLen);
            e = 0;
          }
        }
        for (; mLen >= 8; buffer[offset + i] = m & 255, i += d, m /= 256, mLen -= 8) {
        }
        e = e << mLen | m;
        eLen += mLen;
        for (; eLen > 0; buffer[offset + i] = e & 255, i += d, e /= 256, eLen -= 8) {
        }
        buffer[offset + i - d] |= s2 * 128;
      };
    }
  });

  // node_modules/.pnpm/buffer@6.0.3_patch_hash=zkkuxompt5d553skpnegwi5wuy/node_modules/buffer/index.js
  var require_buffer = __commonJS({
    "node_modules/.pnpm/buffer@6.0.3_patch_hash=zkkuxompt5d553skpnegwi5wuy/node_modules/buffer/index.js"(exports) {
      "use strict";
      init_dist();
      var base64 = require_base64_js();
      var ieee754 = require_ieee754();
      var customInspectSymbol = typeof Symbol === "function" && typeof Symbol["for"] === "function" ? Symbol["for"]("nodejs.util.inspect.custom") : null;
      exports.Buffer = Buffer2;
      exports.SlowBuffer = SlowBuffer;
      exports.INSPECT_MAX_BYTES = 50;
      var K_MAX_LENGTH = 2147483647;
      exports.kMaxLength = K_MAX_LENGTH;
      var { Uint8Array: GlobalUint8Array, ArrayBuffer: GlobalArrayBuffer, SharedArrayBuffer: GlobalSharedArrayBuffer } = globalThis;
      Buffer2.TYPED_ARRAY_SUPPORT = typedArraySupport();
      if (!Buffer2.TYPED_ARRAY_SUPPORT && typeof console !== "undefined" && typeof console.error === "function") {
        console.error(
          "This browser lacks typed array (Uint8Array) support which is required by `buffer` v5.x. Use `buffer` v4.x if you require old browser support."
        );
      }
      function typedArraySupport() {
        try {
          const arr = new GlobalUint8Array(1);
          const proto = { foo: function() {
            return 42;
          } };
          Object.setPrototypeOf(proto, GlobalUint8Array.prototype);
          Object.setPrototypeOf(arr, proto);
          return arr.foo() === 42;
        } catch (e) {
          return false;
        }
      }
      Object.defineProperty(Buffer2.prototype, "parent", {
        enumerable: true,
        get: function() {
          if (!Buffer2.isBuffer(this))
            return void 0;
          return this.buffer;
        }
      });
      Object.defineProperty(Buffer2.prototype, "offset", {
        enumerable: true,
        get: function() {
          if (!Buffer2.isBuffer(this))
            return void 0;
          return this.byteOffset;
        }
      });
      function createBuffer(length) {
        if (length > K_MAX_LENGTH) {
          throw new RangeError('The value "' + length + '" is invalid for option "size"');
        }
        const buf = new GlobalUint8Array(length);
        Object.setPrototypeOf(buf, Buffer2.prototype);
        return buf;
      }
      function Buffer2(arg, encodingOrOffset, length) {
        if (typeof arg === "number") {
          if (typeof encodingOrOffset === "string") {
            throw new TypeError(
              'The "string" argument must be of type string. Received type number'
            );
          }
          return allocUnsafe(arg);
        }
        return from(arg, encodingOrOffset, length);
      }
      Buffer2.poolSize = 8192;
      function from(value, encodingOrOffset, length) {
        if (typeof value === "string") {
          return fromString(value, encodingOrOffset);
        }
        if (GlobalArrayBuffer.isView(value)) {
          return fromArrayView(value);
        }
        if (value == null) {
          throw new TypeError(
            "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
          );
        }
        if (isInstance(value, GlobalArrayBuffer) || value && isInstance(value.buffer, GlobalArrayBuffer)) {
          return fromArrayBuffer(value, encodingOrOffset, length);
        }
        if (typeof GlobalSharedArrayBuffer !== "undefined" && (isInstance(value, GlobalSharedArrayBuffer) || value && isInstance(value.buffer, GlobalSharedArrayBuffer))) {
          return fromArrayBuffer(value, encodingOrOffset, length);
        }
        if (typeof value === "number") {
          throw new TypeError(
            'The "value" argument must not be of type number. Received type number'
          );
        }
        const valueOf = value.valueOf && value.valueOf();
        if (valueOf != null && valueOf !== value) {
          return Buffer2.from(valueOf, encodingOrOffset, length);
        }
        const b = fromObject(value);
        if (b)
          return b;
        if (typeof Symbol !== "undefined" && Symbol.toPrimitive != null && typeof value[Symbol.toPrimitive] === "function") {
          return Buffer2.from(value[Symbol.toPrimitive]("string"), encodingOrOffset, length);
        }
        throw new TypeError(
          "The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof value
        );
      }
      Buffer2.from = function(value, encodingOrOffset, length) {
        return from(value, encodingOrOffset, length);
      };
      Object.setPrototypeOf(Buffer2.prototype, GlobalUint8Array.prototype);
      Object.setPrototypeOf(Buffer2, GlobalUint8Array);
      function assertSize(size) {
        if (typeof size !== "number") {
          throw new TypeError('"size" argument must be of type number');
        } else if (size < 0) {
          throw new RangeError('The value "' + size + '" is invalid for option "size"');
        }
      }
      function alloc(size, fill, encoding) {
        assertSize(size);
        if (size <= 0) {
          return createBuffer(size);
        }
        if (fill !== void 0) {
          return typeof encoding === "string" ? createBuffer(size).fill(fill, encoding) : createBuffer(size).fill(fill);
        }
        return createBuffer(size);
      }
      Buffer2.alloc = function(size, fill, encoding) {
        return alloc(size, fill, encoding);
      };
      function allocUnsafe(size) {
        assertSize(size);
        return createBuffer(size < 0 ? 0 : checked(size) | 0);
      }
      Buffer2.allocUnsafe = function(size) {
        return allocUnsafe(size);
      };
      Buffer2.allocUnsafeSlow = function(size) {
        return allocUnsafe(size);
      };
      function fromString(string, encoding) {
        if (typeof encoding !== "string" || encoding === "") {
          encoding = "utf8";
        }
        if (!Buffer2.isEncoding(encoding)) {
          throw new TypeError("Unknown encoding: " + encoding);
        }
        const length = byteLength(string, encoding) | 0;
        let buf = createBuffer(length);
        const actual = buf.write(string, encoding);
        if (actual !== length) {
          buf = buf.slice(0, actual);
        }
        return buf;
      }
      function fromArrayLike(array) {
        const length = array.length < 0 ? 0 : checked(array.length) | 0;
        const buf = createBuffer(length);
        for (let i = 0; i < length; i += 1) {
          buf[i] = array[i] & 255;
        }
        return buf;
      }
      function fromArrayView(arrayView) {
        if (isInstance(arrayView, GlobalUint8Array)) {
          const copy = new GlobalUint8Array(arrayView);
          return fromArrayBuffer(copy.buffer, copy.byteOffset, copy.byteLength);
        }
        return fromArrayLike(arrayView);
      }
      function fromArrayBuffer(array, byteOffset, length) {
        if (byteOffset < 0 || array.byteLength < byteOffset) {
          throw new RangeError('"offset" is outside of buffer bounds');
        }
        if (array.byteLength < byteOffset + (length || 0)) {
          throw new RangeError('"length" is outside of buffer bounds');
        }
        let buf;
        if (byteOffset === void 0 && length === void 0) {
          buf = new GlobalUint8Array(array);
        } else if (length === void 0) {
          buf = new GlobalUint8Array(array, byteOffset);
        } else {
          buf = new GlobalUint8Array(array, byteOffset, length);
        }
        Object.setPrototypeOf(buf, Buffer2.prototype);
        return buf;
      }
      function fromObject(obj) {
        if (Buffer2.isBuffer(obj)) {
          const len = checked(obj.length) | 0;
          const buf = createBuffer(len);
          if (buf.length === 0) {
            return buf;
          }
          obj.copy(buf, 0, 0, len);
          return buf;
        }
        if (obj.length !== void 0) {
          if (typeof obj.length !== "number" || numberIsNaN(obj.length)) {
            return createBuffer(0);
          }
          return fromArrayLike(obj);
        }
        if (obj.type === "Buffer" && Array.isArray(obj.data)) {
          return fromArrayLike(obj.data);
        }
      }
      function checked(length) {
        if (length >= K_MAX_LENGTH) {
          throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + K_MAX_LENGTH.toString(16) + " bytes");
        }
        return length | 0;
      }
      function SlowBuffer(length) {
        if (+length != length) {
          length = 0;
        }
        return Buffer2.alloc(+length);
      }
      Buffer2.isBuffer = function isBuffer(b) {
        return b != null && b._isBuffer === true && b !== Buffer2.prototype;
      };
      Buffer2.compare = function compare(a, b) {
        if (isInstance(a, GlobalUint8Array))
          a = Buffer2.from(a, a.offset, a.byteLength);
        if (isInstance(b, GlobalUint8Array))
          b = Buffer2.from(b, b.offset, b.byteLength);
        if (!Buffer2.isBuffer(a) || !Buffer2.isBuffer(b)) {
          throw new TypeError(
            'The "buf1", "buf2" arguments must be one of type Buffer or Uint8Array'
          );
        }
        if (a === b)
          return 0;
        let x = a.length;
        let y = b.length;
        for (let i = 0, len = Math.min(x, y); i < len; ++i) {
          if (a[i] !== b[i]) {
            x = a[i];
            y = b[i];
            break;
          }
        }
        if (x < y)
          return -1;
        if (y < x)
          return 1;
        return 0;
      };
      Buffer2.isEncoding = function isEncoding(encoding) {
        switch (String(encoding).toLowerCase()) {
          case "hex":
          case "utf8":
          case "utf-8":
          case "ascii":
          case "latin1":
          case "binary":
          case "base64":
          case "ucs2":
          case "ucs-2":
          case "utf16le":
          case "utf-16le":
            return true;
          default:
            return false;
        }
      };
      Buffer2.concat = function concat(list, length) {
        if (!Array.isArray(list)) {
          throw new TypeError('"list" argument must be an Array of Buffers');
        }
        if (list.length === 0) {
          return Buffer2.alloc(0);
        }
        let i;
        if (length === void 0) {
          length = 0;
          for (i = 0; i < list.length; ++i) {
            length += list[i].length;
          }
        }
        const buffer = Buffer2.allocUnsafe(length);
        let pos = 0;
        for (i = 0; i < list.length; ++i) {
          let buf = list[i];
          if (isInstance(buf, GlobalUint8Array)) {
            if (pos + buf.length > buffer.length) {
              if (!Buffer2.isBuffer(buf))
                buf = Buffer2.from(buf);
              buf.copy(buffer, pos);
            } else {
              GlobalUint8Array.prototype.set.call(
                buffer,
                buf,
                pos
              );
            }
          } else if (!Buffer2.isBuffer(buf)) {
            throw new TypeError('"list" argument must be an Array of Buffers');
          } else {
            buf.copy(buffer, pos);
          }
          pos += buf.length;
        }
        return buffer;
      };
      function byteLength(string, encoding) {
        if (Buffer2.isBuffer(string)) {
          return string.length;
        }
        if (GlobalArrayBuffer.isView(string) || isInstance(string, GlobalArrayBuffer)) {
          return string.byteLength;
        }
        if (typeof string !== "string") {
          throw new TypeError(
            'The "string" argument must be one of type string, Buffer, or ArrayBuffer. Received type ' + typeof string
          );
        }
        const len = string.length;
        const mustMatch = arguments.length > 2 && arguments[2] === true;
        if (!mustMatch && len === 0)
          return 0;
        let loweredCase = false;
        for (; ; ) {
          switch (encoding) {
            case "ascii":
            case "latin1":
            case "binary":
              return len;
            case "utf8":
            case "utf-8":
              return utf8ToBytes(string).length;
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return len * 2;
            case "hex":
              return len >>> 1;
            case "base64":
              return base64ToBytes(string).length;
            default:
              if (loweredCase) {
                return mustMatch ? -1 : utf8ToBytes(string).length;
              }
              encoding = ("" + encoding).toLowerCase();
              loweredCase = true;
          }
        }
      }
      Buffer2.byteLength = byteLength;
      function slowToString(encoding, start, end) {
        let loweredCase = false;
        if (start === void 0 || start < 0) {
          start = 0;
        }
        if (start > this.length) {
          return "";
        }
        if (end === void 0 || end > this.length) {
          end = this.length;
        }
        if (end <= 0) {
          return "";
        }
        end >>>= 0;
        start >>>= 0;
        if (end <= start) {
          return "";
        }
        if (!encoding)
          encoding = "utf8";
        while (true) {
          switch (encoding) {
            case "hex":
              return hexSlice(this, start, end);
            case "utf8":
            case "utf-8":
              return utf8Slice(this, start, end);
            case "ascii":
              return asciiSlice(this, start, end);
            case "latin1":
            case "binary":
              return latin1Slice(this, start, end);
            case "base64":
              return base64Slice(this, start, end);
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return utf16leSlice(this, start, end);
            default:
              if (loweredCase)
                throw new TypeError("Unknown encoding: " + encoding);
              encoding = (encoding + "").toLowerCase();
              loweredCase = true;
          }
        }
      }
      Buffer2.prototype._isBuffer = true;
      function swap(b, n, m) {
        const i = b[n];
        b[n] = b[m];
        b[m] = i;
      }
      Buffer2.prototype.swap16 = function swap16() {
        const len = this.length;
        if (len % 2 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 16-bits");
        }
        for (let i = 0; i < len; i += 2) {
          swap(this, i, i + 1);
        }
        return this;
      };
      Buffer2.prototype.swap32 = function swap32() {
        const len = this.length;
        if (len % 4 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 32-bits");
        }
        for (let i = 0; i < len; i += 4) {
          swap(this, i, i + 3);
          swap(this, i + 1, i + 2);
        }
        return this;
      };
      Buffer2.prototype.swap64 = function swap64() {
        const len = this.length;
        if (len % 8 !== 0) {
          throw new RangeError("Buffer size must be a multiple of 64-bits");
        }
        for (let i = 0; i < len; i += 8) {
          swap(this, i, i + 7);
          swap(this, i + 1, i + 6);
          swap(this, i + 2, i + 5);
          swap(this, i + 3, i + 4);
        }
        return this;
      };
      Buffer2.prototype.toString = function toString() {
        const length = this.length;
        if (length === 0)
          return "";
        if (arguments.length === 0)
          return utf8Slice(this, 0, length);
        return slowToString.apply(this, arguments);
      };
      Buffer2.prototype.toLocaleString = Buffer2.prototype.toString;
      Buffer2.prototype.equals = function equals(b) {
        if (!Buffer2.isBuffer(b))
          throw new TypeError("Argument must be a Buffer");
        if (this === b)
          return true;
        return Buffer2.compare(this, b) === 0;
      };
      Buffer2.prototype.inspect = function inspect() {
        let str = "";
        const max = exports.INSPECT_MAX_BYTES;
        str = this.toString("hex", 0, max).replace(/(.{2})/g, "$1 ").trim();
        if (this.length > max)
          str += " ... ";
        return "<Buffer " + str + ">";
      };
      if (customInspectSymbol) {
        Buffer2.prototype[customInspectSymbol] = Buffer2.prototype.inspect;
      }
      Buffer2.prototype.compare = function compare(target, start, end, thisStart, thisEnd) {
        if (isInstance(target, GlobalUint8Array)) {
          target = Buffer2.from(target, target.offset, target.byteLength);
        }
        if (!Buffer2.isBuffer(target)) {
          throw new TypeError(
            'The "target" argument must be one of type Buffer or Uint8Array. Received type ' + typeof target
          );
        }
        if (start === void 0) {
          start = 0;
        }
        if (end === void 0) {
          end = target ? target.length : 0;
        }
        if (thisStart === void 0) {
          thisStart = 0;
        }
        if (thisEnd === void 0) {
          thisEnd = this.length;
        }
        if (start < 0 || end > target.length || thisStart < 0 || thisEnd > this.length) {
          throw new RangeError("out of range index");
        }
        if (thisStart >= thisEnd && start >= end) {
          return 0;
        }
        if (thisStart >= thisEnd) {
          return -1;
        }
        if (start >= end) {
          return 1;
        }
        start >>>= 0;
        end >>>= 0;
        thisStart >>>= 0;
        thisEnd >>>= 0;
        if (this === target)
          return 0;
        let x = thisEnd - thisStart;
        let y = end - start;
        const len = Math.min(x, y);
        const thisCopy = this.slice(thisStart, thisEnd);
        const targetCopy = target.slice(start, end);
        for (let i = 0; i < len; ++i) {
          if (thisCopy[i] !== targetCopy[i]) {
            x = thisCopy[i];
            y = targetCopy[i];
            break;
          }
        }
        if (x < y)
          return -1;
        if (y < x)
          return 1;
        return 0;
      };
      function bidirectionalIndexOf(buffer, val, byteOffset, encoding, dir) {
        if (buffer.length === 0)
          return -1;
        if (typeof byteOffset === "string") {
          encoding = byteOffset;
          byteOffset = 0;
        } else if (byteOffset > 2147483647) {
          byteOffset = 2147483647;
        } else if (byteOffset < -2147483648) {
          byteOffset = -2147483648;
        }
        byteOffset = +byteOffset;
        if (numberIsNaN(byteOffset)) {
          byteOffset = dir ? 0 : buffer.length - 1;
        }
        if (byteOffset < 0)
          byteOffset = buffer.length + byteOffset;
        if (byteOffset >= buffer.length) {
          if (dir)
            return -1;
          else
            byteOffset = buffer.length - 1;
        } else if (byteOffset < 0) {
          if (dir)
            byteOffset = 0;
          else
            return -1;
        }
        if (typeof val === "string") {
          val = Buffer2.from(val, encoding);
        }
        if (Buffer2.isBuffer(val)) {
          if (val.length === 0) {
            return -1;
          }
          return arrayIndexOf(buffer, val, byteOffset, encoding, dir);
        } else if (typeof val === "number") {
          val = val & 255;
          if (typeof GlobalUint8Array.prototype.indexOf === "function") {
            if (dir) {
              return GlobalUint8Array.prototype.indexOf.call(buffer, val, byteOffset);
            } else {
              return GlobalUint8Array.prototype.lastIndexOf.call(buffer, val, byteOffset);
            }
          }
          return arrayIndexOf(buffer, [val], byteOffset, encoding, dir);
        }
        throw new TypeError("val must be string, number or Buffer");
      }
      function arrayIndexOf(arr, val, byteOffset, encoding, dir) {
        let indexSize = 1;
        let arrLength = arr.length;
        let valLength = val.length;
        if (encoding !== void 0) {
          encoding = String(encoding).toLowerCase();
          if (encoding === "ucs2" || encoding === "ucs-2" || encoding === "utf16le" || encoding === "utf-16le") {
            if (arr.length < 2 || val.length < 2) {
              return -1;
            }
            indexSize = 2;
            arrLength /= 2;
            valLength /= 2;
            byteOffset /= 2;
          }
        }
        function read(buf, i2) {
          if (indexSize === 1) {
            return buf[i2];
          } else {
            return buf.readUInt16BE(i2 * indexSize);
          }
        }
        let i;
        if (dir) {
          let foundIndex = -1;
          for (i = byteOffset; i < arrLength; i++) {
            if (read(arr, i) === read(val, foundIndex === -1 ? 0 : i - foundIndex)) {
              if (foundIndex === -1)
                foundIndex = i;
              if (i - foundIndex + 1 === valLength)
                return foundIndex * indexSize;
            } else {
              if (foundIndex !== -1)
                i -= i - foundIndex;
              foundIndex = -1;
            }
          }
        } else {
          if (byteOffset + valLength > arrLength)
            byteOffset = arrLength - valLength;
          for (i = byteOffset; i >= 0; i--) {
            let found = true;
            for (let j = 0; j < valLength; j++) {
              if (read(arr, i + j) !== read(val, j)) {
                found = false;
                break;
              }
            }
            if (found)
              return i;
          }
        }
        return -1;
      }
      Buffer2.prototype.includes = function includes(val, byteOffset, encoding) {
        return this.indexOf(val, byteOffset, encoding) !== -1;
      };
      Buffer2.prototype.indexOf = function indexOf(val, byteOffset, encoding) {
        return bidirectionalIndexOf(this, val, byteOffset, encoding, true);
      };
      Buffer2.prototype.lastIndexOf = function lastIndexOf(val, byteOffset, encoding) {
        return bidirectionalIndexOf(this, val, byteOffset, encoding, false);
      };
      function hexWrite(buf, string, offset, length) {
        offset = Number(offset) || 0;
        const remaining = buf.length - offset;
        if (!length) {
          length = remaining;
        } else {
          length = Number(length);
          if (length > remaining) {
            length = remaining;
          }
        }
        const strLen = string.length;
        if (length > strLen / 2) {
          length = strLen / 2;
        }
        let i;
        for (i = 0; i < length; ++i) {
          const parsed = parseInt(string.substr(i * 2, 2), 16);
          if (numberIsNaN(parsed))
            return i;
          buf[offset + i] = parsed;
        }
        return i;
      }
      function utf8Write(buf, string, offset, length) {
        return blitBuffer(utf8ToBytes(string, buf.length - offset), buf, offset, length);
      }
      function asciiWrite(buf, string, offset, length) {
        return blitBuffer(asciiToBytes(string), buf, offset, length);
      }
      function base64Write(buf, string, offset, length) {
        return blitBuffer(base64ToBytes(string), buf, offset, length);
      }
      function ucs2Write(buf, string, offset, length) {
        return blitBuffer(utf16leToBytes(string, buf.length - offset), buf, offset, length);
      }
      Buffer2.prototype.write = function write(string, offset, length, encoding) {
        if (offset === void 0) {
          encoding = "utf8";
          length = this.length;
          offset = 0;
        } else if (length === void 0 && typeof offset === "string") {
          encoding = offset;
          length = this.length;
          offset = 0;
        } else if (isFinite(offset)) {
          offset = offset >>> 0;
          if (isFinite(length)) {
            length = length >>> 0;
            if (encoding === void 0)
              encoding = "utf8";
          } else {
            encoding = length;
            length = void 0;
          }
        } else {
          throw new Error(
            "Buffer.write(string, encoding, offset[, length]) is no longer supported"
          );
        }
        const remaining = this.length - offset;
        if (length === void 0 || length > remaining)
          length = remaining;
        if (string.length > 0 && (length < 0 || offset < 0) || offset > this.length) {
          throw new RangeError("Attempt to write outside buffer bounds");
        }
        if (!encoding)
          encoding = "utf8";
        let loweredCase = false;
        for (; ; ) {
          switch (encoding) {
            case "hex":
              return hexWrite(this, string, offset, length);
            case "utf8":
            case "utf-8":
              return utf8Write(this, string, offset, length);
            case "ascii":
            case "latin1":
            case "binary":
              return asciiWrite(this, string, offset, length);
            case "base64":
              return base64Write(this, string, offset, length);
            case "ucs2":
            case "ucs-2":
            case "utf16le":
            case "utf-16le":
              return ucs2Write(this, string, offset, length);
            default:
              if (loweredCase)
                throw new TypeError("Unknown encoding: " + encoding);
              encoding = ("" + encoding).toLowerCase();
              loweredCase = true;
          }
        }
      };
      Buffer2.prototype.toJSON = function toJSON() {
        return {
          type: "Buffer",
          data: Array.prototype.slice.call(this._arr || this, 0)
        };
      };
      function base64Slice(buf, start, end) {
        if (start === 0 && end === buf.length) {
          return base64.fromByteArray(buf);
        } else {
          return base64.fromByteArray(buf.slice(start, end));
        }
      }
      function utf8Slice(buf, start, end) {
        end = Math.min(buf.length, end);
        const res = [];
        let i = start;
        while (i < end) {
          const firstByte = buf[i];
          let codePoint = null;
          let bytesPerSequence = firstByte > 239 ? 4 : firstByte > 223 ? 3 : firstByte > 191 ? 2 : 1;
          if (i + bytesPerSequence <= end) {
            let secondByte, thirdByte, fourthByte, tempCodePoint;
            switch (bytesPerSequence) {
              case 1:
                if (firstByte < 128) {
                  codePoint = firstByte;
                }
                break;
              case 2:
                secondByte = buf[i + 1];
                if ((secondByte & 192) === 128) {
                  tempCodePoint = (firstByte & 31) << 6 | secondByte & 63;
                  if (tempCodePoint > 127) {
                    codePoint = tempCodePoint;
                  }
                }
                break;
              case 3:
                secondByte = buf[i + 1];
                thirdByte = buf[i + 2];
                if ((secondByte & 192) === 128 && (thirdByte & 192) === 128) {
                  tempCodePoint = (firstByte & 15) << 12 | (secondByte & 63) << 6 | thirdByte & 63;
                  if (tempCodePoint > 2047 && (tempCodePoint < 55296 || tempCodePoint > 57343)) {
                    codePoint = tempCodePoint;
                  }
                }
                break;
              case 4:
                secondByte = buf[i + 1];
                thirdByte = buf[i + 2];
                fourthByte = buf[i + 3];
                if ((secondByte & 192) === 128 && (thirdByte & 192) === 128 && (fourthByte & 192) === 128) {
                  tempCodePoint = (firstByte & 15) << 18 | (secondByte & 63) << 12 | (thirdByte & 63) << 6 | fourthByte & 63;
                  if (tempCodePoint > 65535 && tempCodePoint < 1114112) {
                    codePoint = tempCodePoint;
                  }
                }
            }
          }
          if (codePoint === null) {
            codePoint = 65533;
            bytesPerSequence = 1;
          } else if (codePoint > 65535) {
            codePoint -= 65536;
            res.push(codePoint >>> 10 & 1023 | 55296);
            codePoint = 56320 | codePoint & 1023;
          }
          res.push(codePoint);
          i += bytesPerSequence;
        }
        return decodeCodePointsArray(res);
      }
      var MAX_ARGUMENTS_LENGTH = 4096;
      function decodeCodePointsArray(codePoints) {
        const len = codePoints.length;
        if (len <= MAX_ARGUMENTS_LENGTH) {
          return String.fromCharCode.apply(String, codePoints);
        }
        let res = "";
        let i = 0;
        while (i < len) {
          res += String.fromCharCode.apply(
            String,
            codePoints.slice(i, i += MAX_ARGUMENTS_LENGTH)
          );
        }
        return res;
      }
      function asciiSlice(buf, start, end) {
        let ret = "";
        end = Math.min(buf.length, end);
        for (let i = start; i < end; ++i) {
          ret += String.fromCharCode(buf[i] & 127);
        }
        return ret;
      }
      function latin1Slice(buf, start, end) {
        let ret = "";
        end = Math.min(buf.length, end);
        for (let i = start; i < end; ++i) {
          ret += String.fromCharCode(buf[i]);
        }
        return ret;
      }
      function hexSlice(buf, start, end) {
        const len = buf.length;
        if (!start || start < 0)
          start = 0;
        if (!end || end < 0 || end > len)
          end = len;
        let out = "";
        for (let i = start; i < end; ++i) {
          out += hexSliceLookupTable[buf[i]];
        }
        return out;
      }
      function utf16leSlice(buf, start, end) {
        const bytes = buf.slice(start, end);
        let res = "";
        for (let i = 0; i < bytes.length - 1; i += 2) {
          res += String.fromCharCode(bytes[i] + bytes[i + 1] * 256);
        }
        return res;
      }
      Buffer2.prototype.slice = function slice(start, end) {
        const len = this.length;
        start = ~~start;
        end = end === void 0 ? len : ~~end;
        if (start < 0) {
          start += len;
          if (start < 0)
            start = 0;
        } else if (start > len) {
          start = len;
        }
        if (end < 0) {
          end += len;
          if (end < 0)
            end = 0;
        } else if (end > len) {
          end = len;
        }
        if (end < start)
          end = start;
        const newBuf = this.subarray(start, end);
        Object.setPrototypeOf(newBuf, Buffer2.prototype);
        return newBuf;
      };
      function checkOffset(offset, ext, length) {
        if (offset % 1 !== 0 || offset < 0)
          throw new RangeError("offset is not uint");
        if (offset + ext > length)
          throw new RangeError("Trying to access beyond buffer length");
      }
      Buffer2.prototype.readUintLE = Buffer2.prototype.readUIntLE = function readUIntLE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert)
          checkOffset(offset, byteLength2, this.length);
        let val = this[offset];
        let mul = 1;
        let i = 0;
        while (++i < byteLength2 && (mul *= 256)) {
          val += this[offset + i] * mul;
        }
        return val;
      };
      Buffer2.prototype.readUintBE = Buffer2.prototype.readUIntBE = function readUIntBE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          checkOffset(offset, byteLength2, this.length);
        }
        let val = this[offset + --byteLength2];
        let mul = 1;
        while (byteLength2 > 0 && (mul *= 256)) {
          val += this[offset + --byteLength2] * mul;
        }
        return val;
      };
      Buffer2.prototype.readUint8 = Buffer2.prototype.readUInt8 = function readUInt8(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 1, this.length);
        return this[offset];
      };
      Buffer2.prototype.readUint16LE = Buffer2.prototype.readUInt16LE = function readUInt16LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 2, this.length);
        return this[offset] | this[offset + 1] << 8;
      };
      Buffer2.prototype.readUint16BE = Buffer2.prototype.readUInt16BE = function readUInt16BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 2, this.length);
        return this[offset] << 8 | this[offset + 1];
      };
      Buffer2.prototype.readUint32LE = Buffer2.prototype.readUInt32LE = function readUInt32LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return (this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16) + this[offset + 3] * 16777216;
      };
      Buffer2.prototype.readUint32BE = Buffer2.prototype.readUInt32BE = function readUInt32BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return this[offset] * 16777216 + (this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3]);
      };
      Buffer2.prototype.readBigUInt64LE = defineBigIntMethod(function readBigUInt64LE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const lo = first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24;
        const hi = this[++offset] + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + last * 2 ** 24;
        return BigInt(lo) + (BigInt(hi) << BigInt(32));
      });
      Buffer2.prototype.readBigUInt64BE = defineBigIntMethod(function readBigUInt64BE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const hi = first * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
        const lo = this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last;
        return (BigInt(hi) << BigInt(32)) + BigInt(lo);
      });
      Buffer2.prototype.readIntLE = function readIntLE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert)
          checkOffset(offset, byteLength2, this.length);
        let val = this[offset];
        let mul = 1;
        let i = 0;
        while (++i < byteLength2 && (mul *= 256)) {
          val += this[offset + i] * mul;
        }
        mul *= 128;
        if (val >= mul)
          val -= Math.pow(2, 8 * byteLength2);
        return val;
      };
      Buffer2.prototype.readIntBE = function readIntBE(offset, byteLength2, noAssert) {
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert)
          checkOffset(offset, byteLength2, this.length);
        let i = byteLength2;
        let mul = 1;
        let val = this[offset + --i];
        while (i > 0 && (mul *= 256)) {
          val += this[offset + --i] * mul;
        }
        mul *= 128;
        if (val >= mul)
          val -= Math.pow(2, 8 * byteLength2);
        return val;
      };
      Buffer2.prototype.readInt8 = function readInt8(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 1, this.length);
        if (!(this[offset] & 128))
          return this[offset];
        return (255 - this[offset] + 1) * -1;
      };
      Buffer2.prototype.readInt16LE = function readInt16LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 2, this.length);
        const val = this[offset] | this[offset + 1] << 8;
        return val & 32768 ? val | 4294901760 : val;
      };
      Buffer2.prototype.readInt16BE = function readInt16BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 2, this.length);
        const val = this[offset + 1] | this[offset] << 8;
        return val & 32768 ? val | 4294901760 : val;
      };
      Buffer2.prototype.readInt32LE = function readInt32LE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return this[offset] | this[offset + 1] << 8 | this[offset + 2] << 16 | this[offset + 3] << 24;
      };
      Buffer2.prototype.readInt32BE = function readInt32BE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return this[offset] << 24 | this[offset + 1] << 16 | this[offset + 2] << 8 | this[offset + 3];
      };
      Buffer2.prototype.readBigInt64LE = defineBigIntMethod(function readBigInt64LE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const val = this[offset + 4] + this[offset + 5] * 2 ** 8 + this[offset + 6] * 2 ** 16 + (last << 24);
        return (BigInt(val) << BigInt(32)) + BigInt(first + this[++offset] * 2 ** 8 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 24);
      });
      Buffer2.prototype.readBigInt64BE = defineBigIntMethod(function readBigInt64BE(offset) {
        offset = offset >>> 0;
        validateNumber(offset, "offset");
        const first = this[offset];
        const last = this[offset + 7];
        if (first === void 0 || last === void 0) {
          boundsError(offset, this.length - 8);
        }
        const val = (first << 24) + // Overflow
        this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + this[++offset];
        return (BigInt(val) << BigInt(32)) + BigInt(this[++offset] * 2 ** 24 + this[++offset] * 2 ** 16 + this[++offset] * 2 ** 8 + last);
      });
      Buffer2.prototype.readFloatLE = function readFloatLE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return ieee754.read(this, offset, true, 23, 4);
      };
      Buffer2.prototype.readFloatBE = function readFloatBE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 4, this.length);
        return ieee754.read(this, offset, false, 23, 4);
      };
      Buffer2.prototype.readDoubleLE = function readDoubleLE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 8, this.length);
        return ieee754.read(this, offset, true, 52, 8);
      };
      Buffer2.prototype.readDoubleBE = function readDoubleBE(offset, noAssert) {
        offset = offset >>> 0;
        if (!noAssert)
          checkOffset(offset, 8, this.length);
        return ieee754.read(this, offset, false, 52, 8);
      };
      function checkInt(buf, value, offset, ext, max, min) {
        if (!Buffer2.isBuffer(buf))
          throw new TypeError('"buffer" argument must be a Buffer instance');
        if (value > max || value < min)
          throw new RangeError('"value" argument is out of bounds');
        if (offset + ext > buf.length)
          throw new RangeError("Index out of range");
      }
      Buffer2.prototype.writeUintLE = Buffer2.prototype.writeUIntLE = function writeUIntLE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
          checkInt(this, value, offset, byteLength2, maxBytes, 0);
        }
        let mul = 1;
        let i = 0;
        this[offset] = value & 255;
        while (++i < byteLength2 && (mul *= 256)) {
          this[offset + i] = value / mul & 255;
        }
        return offset + byteLength2;
      };
      Buffer2.prototype.writeUintBE = Buffer2.prototype.writeUIntBE = function writeUIntBE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        byteLength2 = byteLength2 >>> 0;
        if (!noAssert) {
          const maxBytes = Math.pow(2, 8 * byteLength2) - 1;
          checkInt(this, value, offset, byteLength2, maxBytes, 0);
        }
        let i = byteLength2 - 1;
        let mul = 1;
        this[offset + i] = value & 255;
        while (--i >= 0 && (mul *= 256)) {
          this[offset + i] = value / mul & 255;
        }
        return offset + byteLength2;
      };
      Buffer2.prototype.writeUint8 = Buffer2.prototype.writeUInt8 = function writeUInt8(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 1, 255, 0);
        this[offset] = value & 255;
        return offset + 1;
      };
      Buffer2.prototype.writeUint16LE = Buffer2.prototype.writeUInt16LE = function writeUInt16LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 2, 65535, 0);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        return offset + 2;
      };
      Buffer2.prototype.writeUint16BE = Buffer2.prototype.writeUInt16BE = function writeUInt16BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 2, 65535, 0);
        this[offset] = value >>> 8;
        this[offset + 1] = value & 255;
        return offset + 2;
      };
      Buffer2.prototype.writeUint32LE = Buffer2.prototype.writeUInt32LE = function writeUInt32LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 4, 4294967295, 0);
        this[offset + 3] = value >>> 24;
        this[offset + 2] = value >>> 16;
        this[offset + 1] = value >>> 8;
        this[offset] = value & 255;
        return offset + 4;
      };
      Buffer2.prototype.writeUint32BE = Buffer2.prototype.writeUInt32BE = function writeUInt32BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 4, 4294967295, 0);
        this[offset] = value >>> 24;
        this[offset + 1] = value >>> 16;
        this[offset + 2] = value >>> 8;
        this[offset + 3] = value & 255;
        return offset + 4;
      };
      function wrtBigUInt64LE(buf, value, offset, min, max) {
        checkIntBI(value, min, max, buf, offset, 7);
        let lo = Number(value & BigInt(4294967295));
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        lo = lo >> 8;
        buf[offset++] = lo;
        let hi = Number(value >> BigInt(32) & BigInt(4294967295));
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        hi = hi >> 8;
        buf[offset++] = hi;
        return offset;
      }
      function wrtBigUInt64BE(buf, value, offset, min, max) {
        checkIntBI(value, min, max, buf, offset, 7);
        let lo = Number(value & BigInt(4294967295));
        buf[offset + 7] = lo;
        lo = lo >> 8;
        buf[offset + 6] = lo;
        lo = lo >> 8;
        buf[offset + 5] = lo;
        lo = lo >> 8;
        buf[offset + 4] = lo;
        let hi = Number(value >> BigInt(32) & BigInt(4294967295));
        buf[offset + 3] = hi;
        hi = hi >> 8;
        buf[offset + 2] = hi;
        hi = hi >> 8;
        buf[offset + 1] = hi;
        hi = hi >> 8;
        buf[offset] = hi;
        return offset + 8;
      }
      Buffer2.prototype.writeBigUInt64LE = defineBigIntMethod(function writeBigUInt64LE(value, offset = 0) {
        return wrtBigUInt64LE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
      });
      Buffer2.prototype.writeBigUInt64BE = defineBigIntMethod(function writeBigUInt64BE(value, offset = 0) {
        return wrtBigUInt64BE(this, value, offset, BigInt(0), BigInt("0xffffffffffffffff"));
      });
      Buffer2.prototype.writeIntLE = function writeIntLE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          const limit = Math.pow(2, 8 * byteLength2 - 1);
          checkInt(this, value, offset, byteLength2, limit - 1, -limit);
        }
        let i = 0;
        let mul = 1;
        let sub = 0;
        this[offset] = value & 255;
        while (++i < byteLength2 && (mul *= 256)) {
          if (value < 0 && sub === 0 && this[offset + i - 1] !== 0) {
            sub = 1;
          }
          this[offset + i] = (value / mul >> 0) - sub & 255;
        }
        return offset + byteLength2;
      };
      Buffer2.prototype.writeIntBE = function writeIntBE(value, offset, byteLength2, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          const limit = Math.pow(2, 8 * byteLength2 - 1);
          checkInt(this, value, offset, byteLength2, limit - 1, -limit);
        }
        let i = byteLength2 - 1;
        let mul = 1;
        let sub = 0;
        this[offset + i] = value & 255;
        while (--i >= 0 && (mul *= 256)) {
          if (value < 0 && sub === 0 && this[offset + i + 1] !== 0) {
            sub = 1;
          }
          this[offset + i] = (value / mul >> 0) - sub & 255;
        }
        return offset + byteLength2;
      };
      Buffer2.prototype.writeInt8 = function writeInt8(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 1, 127, -128);
        if (value < 0)
          value = 255 + value + 1;
        this[offset] = value & 255;
        return offset + 1;
      };
      Buffer2.prototype.writeInt16LE = function writeInt16LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 2, 32767, -32768);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        return offset + 2;
      };
      Buffer2.prototype.writeInt16BE = function writeInt16BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 2, 32767, -32768);
        this[offset] = value >>> 8;
        this[offset + 1] = value & 255;
        return offset + 2;
      };
      Buffer2.prototype.writeInt32LE = function writeInt32LE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 4, 2147483647, -2147483648);
        this[offset] = value & 255;
        this[offset + 1] = value >>> 8;
        this[offset + 2] = value >>> 16;
        this[offset + 3] = value >>> 24;
        return offset + 4;
      };
      Buffer2.prototype.writeInt32BE = function writeInt32BE(value, offset, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert)
          checkInt(this, value, offset, 4, 2147483647, -2147483648);
        if (value < 0)
          value = 4294967295 + value + 1;
        this[offset] = value >>> 24;
        this[offset + 1] = value >>> 16;
        this[offset + 2] = value >>> 8;
        this[offset + 3] = value & 255;
        return offset + 4;
      };
      Buffer2.prototype.writeBigInt64LE = defineBigIntMethod(function writeBigInt64LE(value, offset = 0) {
        return wrtBigUInt64LE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
      });
      Buffer2.prototype.writeBigInt64BE = defineBigIntMethod(function writeBigInt64BE(value, offset = 0) {
        return wrtBigUInt64BE(this, value, offset, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
      });
      function checkIEEE754(buf, value, offset, ext, max, min) {
        if (offset + ext > buf.length)
          throw new RangeError("Index out of range");
        if (offset < 0)
          throw new RangeError("Index out of range");
      }
      function writeFloat(buf, value, offset, littleEndian, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          checkIEEE754(buf, value, offset, 4, 34028234663852886e22, -34028234663852886e22);
        }
        ieee754.write(buf, value, offset, littleEndian, 23, 4);
        return offset + 4;
      }
      Buffer2.prototype.writeFloatLE = function writeFloatLE(value, offset, noAssert) {
        return writeFloat(this, value, offset, true, noAssert);
      };
      Buffer2.prototype.writeFloatBE = function writeFloatBE(value, offset, noAssert) {
        return writeFloat(this, value, offset, false, noAssert);
      };
      function writeDouble(buf, value, offset, littleEndian, noAssert) {
        value = +value;
        offset = offset >>> 0;
        if (!noAssert) {
          checkIEEE754(buf, value, offset, 8, 17976931348623157e292, -17976931348623157e292);
        }
        ieee754.write(buf, value, offset, littleEndian, 52, 8);
        return offset + 8;
      }
      Buffer2.prototype.writeDoubleLE = function writeDoubleLE(value, offset, noAssert) {
        return writeDouble(this, value, offset, true, noAssert);
      };
      Buffer2.prototype.writeDoubleBE = function writeDoubleBE(value, offset, noAssert) {
        return writeDouble(this, value, offset, false, noAssert);
      };
      Buffer2.prototype.copy = function copy(target, targetStart, start, end) {
        if (!Buffer2.isBuffer(target))
          throw new TypeError("argument should be a Buffer");
        if (!start)
          start = 0;
        if (!end && end !== 0)
          end = this.length;
        if (targetStart >= target.length)
          targetStart = target.length;
        if (!targetStart)
          targetStart = 0;
        if (end > 0 && end < start)
          end = start;
        if (end === start)
          return 0;
        if (target.length === 0 || this.length === 0)
          return 0;
        if (targetStart < 0) {
          throw new RangeError("targetStart out of bounds");
        }
        if (start < 0 || start >= this.length)
          throw new RangeError("Index out of range");
        if (end < 0)
          throw new RangeError("sourceEnd out of bounds");
        if (end > this.length)
          end = this.length;
        if (target.length - targetStart < end - start) {
          end = target.length - targetStart + start;
        }
        const len = end - start;
        if (this === target && typeof GlobalUint8Array.prototype.copyWithin === "function") {
          this.copyWithin(targetStart, start, end);
        } else {
          GlobalUint8Array.prototype.set.call(
            target,
            this.subarray(start, end),
            targetStart
          );
        }
        return len;
      };
      Buffer2.prototype.fill = function fill(val, start, end, encoding) {
        if (typeof val === "string") {
          if (typeof start === "string") {
            encoding = start;
            start = 0;
            end = this.length;
          } else if (typeof end === "string") {
            encoding = end;
            end = this.length;
          }
          if (encoding !== void 0 && typeof encoding !== "string") {
            throw new TypeError("encoding must be a string");
          }
          if (typeof encoding === "string" && !Buffer2.isEncoding(encoding)) {
            throw new TypeError("Unknown encoding: " + encoding);
          }
          if (val.length === 1) {
            const code = val.charCodeAt(0);
            if (encoding === "utf8" && code < 128 || encoding === "latin1") {
              val = code;
            }
          }
        } else if (typeof val === "number") {
          val = val & 255;
        } else if (typeof val === "boolean") {
          val = Number(val);
        }
        if (start < 0 || this.length < start || this.length < end) {
          throw new RangeError("Out of range index");
        }
        if (end <= start) {
          return this;
        }
        start = start >>> 0;
        end = end === void 0 ? this.length : end >>> 0;
        if (!val)
          val = 0;
        let i;
        if (typeof val === "number") {
          for (i = start; i < end; ++i) {
            this[i] = val;
          }
        } else {
          const bytes = Buffer2.isBuffer(val) ? val : Buffer2.from(val, encoding);
          const len = bytes.length;
          if (len === 0) {
            throw new TypeError('The value "' + val + '" is invalid for argument "value"');
          }
          for (i = 0; i < end - start; ++i) {
            this[i + start] = bytes[i % len];
          }
        }
        return this;
      };
      var errors = {};
      function E(sym, getMessage, Base) {
        errors[sym] = class NodeError extends Base {
          constructor() {
            super();
            Object.defineProperty(this, "message", {
              value: getMessage.apply(this, arguments),
              writable: true,
              configurable: true
            });
            this.name = `${this.name} [${sym}]`;
            this.stack;
            delete this.name;
          }
          get code() {
            return sym;
          }
          set code(value) {
            Object.defineProperty(this, "code", {
              configurable: true,
              enumerable: true,
              value,
              writable: true
            });
          }
          toString() {
            return `${this.name} [${sym}]: ${this.message}`;
          }
        };
      }
      E(
        "ERR_BUFFER_OUT_OF_BOUNDS",
        function(name) {
          if (name) {
            return `${name} is outside of buffer bounds`;
          }
          return "Attempt to access memory outside buffer bounds";
        },
        RangeError
      );
      E(
        "ERR_INVALID_ARG_TYPE",
        function(name, actual) {
          return `The "${name}" argument must be of type number. Received type ${typeof actual}`;
        },
        TypeError
      );
      E(
        "ERR_OUT_OF_RANGE",
        function(str, range, input) {
          let msg = `The value of "${str}" is out of range.`;
          let received = input;
          if (Number.isInteger(input) && Math.abs(input) > 2 ** 32) {
            received = addNumericalSeparator(String(input));
          } else if (typeof input === "bigint") {
            received = String(input);
            if (input > BigInt(2) ** BigInt(32) || input < -(BigInt(2) ** BigInt(32))) {
              received = addNumericalSeparator(received);
            }
            received += "n";
          }
          msg += ` It must be ${range}. Received ${received}`;
          return msg;
        },
        RangeError
      );
      function addNumericalSeparator(val) {
        let res = "";
        let i = val.length;
        const start = val[0] === "-" ? 1 : 0;
        for (; i >= start + 4; i -= 3) {
          res = `_${val.slice(i - 3, i)}${res}`;
        }
        return `${val.slice(0, i)}${res}`;
      }
      function checkBounds(buf, offset, byteLength2) {
        validateNumber(offset, "offset");
        if (buf[offset] === void 0 || buf[offset + byteLength2] === void 0) {
          boundsError(offset, buf.length - (byteLength2 + 1));
        }
      }
      function checkIntBI(value, min, max, buf, offset, byteLength2) {
        if (value > max || value < min) {
          const n = typeof min === "bigint" ? "n" : "";
          let range;
          if (byteLength2 > 3) {
            if (min === 0 || min === BigInt(0)) {
              range = `>= 0${n} and < 2${n} ** ${(byteLength2 + 1) * 8}${n}`;
            } else {
              range = `>= -(2${n} ** ${(byteLength2 + 1) * 8 - 1}${n}) and < 2 ** ${(byteLength2 + 1) * 8 - 1}${n}`;
            }
          } else {
            range = `>= ${min}${n} and <= ${max}${n}`;
          }
          throw new errors.ERR_OUT_OF_RANGE("value", range, value);
        }
        checkBounds(buf, offset, byteLength2);
      }
      function validateNumber(value, name) {
        if (typeof value !== "number") {
          throw new errors.ERR_INVALID_ARG_TYPE(name, "number", value);
        }
      }
      function boundsError(value, length, type) {
        if (Math.floor(value) !== value) {
          validateNumber(value, type);
          throw new errors.ERR_OUT_OF_RANGE(type || "offset", "an integer", value);
        }
        if (length < 0) {
          throw new errors.ERR_BUFFER_OUT_OF_BOUNDS();
        }
        throw new errors.ERR_OUT_OF_RANGE(
          type || "offset",
          `>= ${type ? 1 : 0} and <= ${length}`,
          value
        );
      }
      var INVALID_BASE64_RE = /[^+/0-9A-Za-z-_]/g;
      function base64clean(str) {
        str = str.split("=")[0];
        str = str.trim().replace(INVALID_BASE64_RE, "");
        if (str.length < 2)
          return "";
        while (str.length % 4 !== 0) {
          str = str + "=";
        }
        return str;
      }
      function utf8ToBytes(string, units) {
        units = units || Infinity;
        let codePoint;
        const length = string.length;
        let leadSurrogate = null;
        const bytes = [];
        for (let i = 0; i < length; ++i) {
          codePoint = string.charCodeAt(i);
          if (codePoint > 55295 && codePoint < 57344) {
            if (!leadSurrogate) {
              if (codePoint > 56319) {
                if ((units -= 3) > -1)
                  bytes.push(239, 191, 189);
                continue;
              } else if (i + 1 === length) {
                if ((units -= 3) > -1)
                  bytes.push(239, 191, 189);
                continue;
              }
              leadSurrogate = codePoint;
              continue;
            }
            if (codePoint < 56320) {
              if ((units -= 3) > -1)
                bytes.push(239, 191, 189);
              leadSurrogate = codePoint;
              continue;
            }
            codePoint = (leadSurrogate - 55296 << 10 | codePoint - 56320) + 65536;
          } else if (leadSurrogate) {
            if ((units -= 3) > -1)
              bytes.push(239, 191, 189);
          }
          leadSurrogate = null;
          if (codePoint < 128) {
            if ((units -= 1) < 0)
              break;
            bytes.push(codePoint);
          } else if (codePoint < 2048) {
            if ((units -= 2) < 0)
              break;
            bytes.push(
              codePoint >> 6 | 192,
              codePoint & 63 | 128
            );
          } else if (codePoint < 65536) {
            if ((units -= 3) < 0)
              break;
            bytes.push(
              codePoint >> 12 | 224,
              codePoint >> 6 & 63 | 128,
              codePoint & 63 | 128
            );
          } else if (codePoint < 1114112) {
            if ((units -= 4) < 0)
              break;
            bytes.push(
              codePoint >> 18 | 240,
              codePoint >> 12 & 63 | 128,
              codePoint >> 6 & 63 | 128,
              codePoint & 63 | 128
            );
          } else {
            throw new Error("Invalid code point");
          }
        }
        return bytes;
      }
      function asciiToBytes(str) {
        const byteArray = [];
        for (let i = 0; i < str.length; ++i) {
          byteArray.push(str.charCodeAt(i) & 255);
        }
        return byteArray;
      }
      function utf16leToBytes(str, units) {
        let c, hi, lo;
        const byteArray = [];
        for (let i = 0; i < str.length; ++i) {
          if ((units -= 2) < 0)
            break;
          c = str.charCodeAt(i);
          hi = c >> 8;
          lo = c % 256;
          byteArray.push(lo);
          byteArray.push(hi);
        }
        return byteArray;
      }
      function base64ToBytes(str) {
        return base64.toByteArray(base64clean(str));
      }
      function blitBuffer(src, dst, offset, length) {
        let i;
        for (i = 0; i < length; ++i) {
          if (i + offset >= dst.length || i >= src.length)
            break;
          dst[i + offset] = src[i];
        }
        return i;
      }
      function isInstance(obj, type) {
        return obj instanceof type || obj != null && obj.constructor != null && obj.constructor.name != null && obj.constructor.name === type.name;
      }
      function numberIsNaN(obj) {
        return obj !== obj;
      }
      var hexSliceLookupTable = function() {
        const alphabet = "0123456789abcdef";
        const table = new Array(256);
        for (let i = 0; i < 16; ++i) {
          const i16 = i * 16;
          for (let j = 0; j < 16; ++j) {
            table[i16 + j] = alphabet[i] + alphabet[j];
          }
        }
        return table;
      }();
      function defineBigIntMethod(fn) {
        return typeof BigInt === "undefined" ? BufferBigIntNotDefined : fn;
      }
      function BufferBigIntNotDefined() {
        throw new Error("BigInt not supported");
      }
    }
  });

  // node_modules/.pnpm/process@0.11.10/node_modules/process/browser.js
  var require_browser = __commonJS({
    "node_modules/.pnpm/process@0.11.10/node_modules/process/browser.js"(exports, module) {
      init_dist();
      var process = module.exports = {};
      var cachedSetTimeout;
      var cachedClearTimeout;
      function defaultSetTimout() {
        throw new Error("setTimeout has not been defined");
      }
      function defaultClearTimeout() {
        throw new Error("clearTimeout has not been defined");
      }
      (function() {
        try {
          if (typeof setTimeout === "function") {
            cachedSetTimeout = setTimeout;
          } else {
            cachedSetTimeout = defaultSetTimout;
          }
        } catch (e) {
          cachedSetTimeout = defaultSetTimout;
        }
        try {
          if (typeof clearTimeout === "function") {
            cachedClearTimeout = clearTimeout;
          } else {
            cachedClearTimeout = defaultClearTimeout;
          }
        } catch (e) {
          cachedClearTimeout = defaultClearTimeout;
        }
      })();
      function runTimeout(fun) {
        if (cachedSetTimeout === setTimeout) {
          return setTimeout(fun, 0);
        }
        if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
          cachedSetTimeout = setTimeout;
          return setTimeout(fun, 0);
        }
        try {
          return cachedSetTimeout(fun, 0);
        } catch (e) {
          try {
            return cachedSetTimeout.call(null, fun, 0);
          } catch (e2) {
            return cachedSetTimeout.call(this, fun, 0);
          }
        }
      }
      function runClearTimeout(marker) {
        if (cachedClearTimeout === clearTimeout) {
          return clearTimeout(marker);
        }
        if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
          cachedClearTimeout = clearTimeout;
          return clearTimeout(marker);
        }
        try {
          return cachedClearTimeout(marker);
        } catch (e) {
          try {
            return cachedClearTimeout.call(null, marker);
          } catch (e2) {
            return cachedClearTimeout.call(this, marker);
          }
        }
      }
      var queue = [];
      var draining = false;
      var currentQueue;
      var queueIndex = -1;
      function cleanUpNextTick() {
        if (!draining || !currentQueue) {
          return;
        }
        draining = false;
        if (currentQueue.length) {
          queue = currentQueue.concat(queue);
        } else {
          queueIndex = -1;
        }
        if (queue.length) {
          drainQueue();
        }
      }
      function drainQueue() {
        if (draining) {
          return;
        }
        var timeout = runTimeout(cleanUpNextTick);
        draining = true;
        var len = queue.length;
        while (len) {
          currentQueue = queue;
          queue = [];
          while (++queueIndex < len) {
            if (currentQueue) {
              currentQueue[queueIndex].run();
            }
          }
          queueIndex = -1;
          len = queue.length;
        }
        currentQueue = null;
        draining = false;
        runClearTimeout(timeout);
      }
      process.nextTick = function(fun) {
        var args = new Array(arguments.length - 1);
        if (arguments.length > 1) {
          for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
          }
        }
        queue.push(new Item(fun, args));
        if (queue.length === 1 && !draining) {
          runTimeout(drainQueue);
        }
      };
      function Item(fun, array) {
        this.fun = fun;
        this.array = array;
      }
      Item.prototype.run = function() {
        this.fun.apply(null, this.array);
      };
      process.title = "browser";
      process.browser = true;
      process.env = {};
      process.argv = [];
      process.version = "";
      process.versions = {};
      function noop() {
      }
      process.on = noop;
      process.addListener = noop;
      process.once = noop;
      process.off = noop;
      process.removeListener = noop;
      process.removeAllListeners = noop;
      process.emit = noop;
      process.prependListener = noop;
      process.prependOnceListener = noop;
      process.listeners = function(name) {
        return [];
      };
      process.binding = function(name) {
        throw new Error("process.binding is not supported");
      };
      process.cwd = function() {
        return "/";
      };
      process.chdir = function(dir) {
        throw new Error("process.chdir is not supported");
      };
      process.umask = function() {
        return 0;
      };
    }
  });

  // shims/dist/index.js
  var import_buffer_polyfill, import_process, o;
  var init_dist = __esm({
    "shims/dist/index.js"() {
      "use strict";
      import_buffer_polyfill = __toESM(require_buffer(), 1);
      import_process = __toESM(require_browser(), 1);
      o = globalThis || void 0 || self;
    }
  });

  // shims/banner/index.cjs
  var require_banner = __commonJS({
    "shims/banner/index.cjs"() {
      init_dist();
      globalThis.Buffer = globalThis.Buffer || import_buffer_polyfill.Buffer;
      globalThis.global = globalThis.global || o;
      globalThis.process = globalThis.process || import_process.default;
    }
  });
  require_banner();
})();
/*! Bundled license information:

ieee754/index.js:
  (*! ieee754. BSD-3-Clause License. Feross Aboukhadijeh <https://feross.org/opensource> *)

buffer/index.js:
  (*!
   * The buffer module from node.js, for the browser.
   *
   * @author   Feross Aboukhadijeh <https://feross.org>
   * @license  MIT
   *)
*/

import {
  n as n5,
  s as s5
} from "./chunk-LTVTJVOQ.js";
import {
  c as c2,
  s as s2
} from "./chunk-E4HRFNDN.js";
import {
  n as n3,
  r as r2
} from "./chunk-EKM4VSTQ.js";
import {
  i as i4,
  s as s3
} from "./chunk-2TWRSFX4.js";
import {
  n as n4,
  s as s4
} from "./chunk-272VZJHM.js";
import "./chunk-VIOT7UML.js";
import "./chunk-PY4NSTUI.js";
import {
  Z
} from "./chunk-FYW6Q6M7.js";
import "./chunk-63WAZA6B.js";
import "./chunk-UCYQURGC.js";
import {
  G
} from "./chunk-7JEIBMIB.js";
import "./chunk-KREOLAJN.js";
import {
  D as D3
} from "./chunk-MMXUKKUP.js";
import "./chunk-Q3U7FQDK.js";
import {
  H as H2
} from "./chunk-Q362JLS5.js";
import "./chunk-RAJMEEZK.js";
import "./chunk-RFACKTII.js";
import "./chunk-LLB2KFSE.js";
import {
  p2 as p6
} from "./chunk-3TLFSX6D.js";
import "./chunk-PT4ZAT7W.js";
import {
  faBan,
  faCheck,
  faCircleNotch,
  faHourglass,
  faInfo,
  faTimes
} from "./chunk-TXMYIQV3.js";
import "./chunk-3MNASYTU.js";
import "./chunk-63SUVNQE.js";
import {
  require_classnames,
  u as u3,
  v as v3
} from "./chunk-S677S367.js";
import "./chunk-UYE5PNS3.js";
import "./chunk-I55AVBBH.js";
import "./chunk-QJNSLYW7.js";
import "./chunk-Q23JG3F6.js";
import "./chunk-FKHV3UFA.js";
import "./chunk-54JZU37V.js";
import "./chunk-X6W6HIIL.js";
import "./chunk-OUHPLTKQ.js";
import "./chunk-UUNB4OK2.js";
import {
  p as p4
} from "./chunk-4HAL3YZK.js";
import "./chunk-DANFAIW7.js";
import "./chunk-LS4IVGTJ.js";
import {
  h as h2
} from "./chunk-XWYMCR5D.js";
import "./chunk-EPZIXHO6.js";
import "./chunk-LWXYSBFU.js";
import "./chunk-KMNHALK3.js";
import "./chunk-MMRK53YU.js";
import {
  p as p5
} from "./chunk-KL3RVTCS.js";
import "./chunk-FUWKOYH5.js";
import "./chunk-3T2RS5VX.js";
import "./chunk-TZGAA2VZ.js";
import {
  D2,
  H,
  I,
  i as i3,
  v as v2
} from "./chunk-B2F7MDWY.js";
import "./chunk-2DAT54DZ.js";
import "./chunk-W6IS4BBY.js";
import "./chunk-JRAXXMAO.js";
import "./chunk-P6UDNJ3P.js";
import "./chunk-VC3CYCM2.js";
import "./chunk-JTQZEZNW.js";
import "./chunk-3TFE24LV.js";
import "./chunk-WYYX7HOH.js";
import "./chunk-5UC3SJWB.js";
import "./chunk-OQ2X2I36.js";
import "./chunk-REBRBBAX.js";
import "./chunk-4Y4NHRAJ.js";
import "./chunk-X74RERH2.js";
import "./chunk-X4QWOPZU.js";
import "./chunk-KIDRYMT6.js";
import "./chunk-PNCNEUDJ.js";
import "./chunk-4PEQQYYX.js";
import "./chunk-NLJ5ZWYL.js";
import "./chunk-NWWRXY5Z.js";
import "./chunk-Q76JCXYQ.js";
import "./chunk-DLUMZFAB.js";
import "./chunk-Q2ALSPT4.js";
import "./chunk-GWYV5Q5U.js";
import "./chunk-MLGEBXDN.js";
import "./chunk-4JMTGKNX.js";
import "./chunk-46YI7JI6.js";
import "./chunk-KBWKCXSB.js";
import "./chunk-BXH4IYHA.js";
import {
  s
} from "./chunk-CQ6QEUJE.js";
import {
  B2 as B,
  S2 as S,
  c,
  d5 as d2,
  i3 as i2,
  p as p2,
  p3,
  y3 as y2
} from "./chunk-DZSHGTAC.js";
import {
  q as q2
} from "./chunk-6OCHV4EV.js";
import "./chunk-EN76Z23Y.js";
import "./chunk-GT32YW3B.js";
import "./chunk-LOZL6BFA.js";
import {
  D,
  m
} from "./chunk-UF3DAKWQ.js";
import "./chunk-L7SBMHDO.js";
import "./chunk-KMSODH52.js";
import "./chunk-36Z5UFBK.js";
import {
  d,
  k as k3
} from "./chunk-EBUGIFUX.js";
import {
  require_react_dom
} from "./chunk-5MLKHMR6.js";
import "./chunk-WZ74NK6U.js";
import "./chunk-T74HIW6S.js";
import {
  a,
  e,
  f4 as f,
  g2 as g,
  g4 as g2,
  h,
  i2 as i,
  j,
  j2,
  k,
  k2,
  l3 as l,
  n,
  n2,
  o2 as o,
  p5 as p,
  q,
  r,
  y
} from "./chunk-DODOUSNB.js";
import "./chunk-TILBIW7H.js";
import "./chunk-3TFTH3JT.js";
import {
  FontAwesomeIcon
} from "./chunk-7GJK4ZO7.js";
import "./chunk-66L4EHKV.js";
import "./chunk-IXILR3JC.js";
import "./chunk-N4UJNUUL.js";
import {
  u
} from "./chunk-FFVP3WMN.js";
import {
  T2 as T
} from "./chunk-HXV3SQ6S.js";
import {
  u as u2
} from "./chunk-EKHLTR53.js";
import {
  O,
  W
} from "./chunk-N7F464ZJ.js";
import {
  gt,
  v,
  xt,
  yt
} from "./chunk-5CKZHQYH.js";
import {
  require_react
} from "./chunk-IRELMTUJ.js";
import "./chunk-5HSAGO3O.js";
import "./chunk-UHKY3R7I.js";
import "./chunk-C426NY4X.js";
import "./chunk-7IOKNUEZ.js";
import "./chunk-PA4YGARE.js";
import "./chunk-FOX6T53T.js";
import "./chunk-EPGUFONN.js";
import "./chunk-KLHSS55O.js";
import "./chunk-JDLAKVPX.js";
import "./chunk-7T325JRE.js";
import {
  __toESM,
  require_dist
} from "./chunk-UOCZJ3B4.js";

// node_modules/@multiversx/sdk-dapp/UI/TransactionsToastList/TransactionsToastList.js
var import_dist36 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-J5BVHYAZ.js
var import_dist23 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-RWU2GT72.js
var import_dist10 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-7PSZY4BT.js
var import_dist9 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-IZRNK3LN.js
var import_dist2 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-BZJTV2NI.js
var import_dist = __toESM(require_dist());
v();
u2();
var q3 = ({ address: T6, classes: t5 = { success: "success", danger: "danger", warning: "warning" }, sender: m8, status: C3, toastId: s7, transactionDisplayInfo: e4 }) => {
  var r3, i8, u5, n9, c3;
  let S4 = { id: s7, icon: faCheck, expires: 3e4, hasCloseButton: true, title: (r3 = e4 == null ? void 0 : e4.successMessage) != null ? r3 : "Transaction successful", iconClassName: t5.success }, h3 = { id: s7, icon: faCheck, expires: 3e4, hasCloseButton: true, title: "Transaction received", iconClassName: t5.success }, a4 = { id: s7, expires: false, icon: faHourglass, hasCloseButton: false, title: (i8 = e4 == null ? void 0 : e4.processingMessage) != null ? i8 : "Processing transaction", iconClassName: t5.warning }, B3 = { id: s7, icon: faTimes, title: (u5 = e4 == null ? void 0 : e4.errorMessage) != null ? u5 : "Transaction failed", hasCloseButton: true, iconClassName: t5.danger }, l2 = { id: s7, icon: faBan, title: (n9 = e4 == null ? void 0 : e4.invalidMessage) != null ? n9 : "Transaction invalid", hasCloseButton: true, iconClassName: t5 == null ? void 0 : t5.warning }, v6 = { id: s7, icon: faTimes, title: (c3 = e4 == null ? void 0 : e4.timedOutMessage) != null ? c3 : "Transaction timed out", hasCloseButton: true, iconClassName: t5.warning };
  switch (C3) {
    case "signed":
    case "sent":
      return a4;
    case "success":
      return m8 !== T6 ? h3 : S4;
    case "cancelled":
    case "fail":
      return B3;
    case "timedOut":
      return v6;
    case "invalid":
      return l2;
    default:
      return a4;
  }
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-IZRNK3LN.js
var import_react = __toESM(require_react());
v();
T();
S();
n();
n2();
p2();
g();
var St = ({ toastId: s7, transactions: t5, status: r3, lifetimeAfterSuccess: n9, startTimestamp: M2, endTimeProgress: N2, onDelete: i8 }) => {
  let { styles: a4 } = v3({ ssrImportCallback: () => import("./transactionToast.styles-7XAHICMD.js"), clientImportCallback: () => (c2(), gt(s2)).default }), o4 = p6(s7), u5 = k3(B), { address: T6 } = s(), e4 = (0, import_react.useRef)(), d6 = (0, import_react.useMemo)(() => y2(t5, u5), [t5, u5]), b = d6 ? 6e3 : 5 * 6e3, k5 = (o4 == null ? void 0 : o4.transactionDuration) || b, [F2, j3] = (0, import_react.useMemo)(() => {
    let V = M2 || i(), W3 = N2 || o(k5);
    return [V, W3];
  }, []), q5 = { startTime: F2, endTime: j3 }, y3 = j2(r3), B3 = q(r3), H3 = k2(r3), p11 = y(r3), f4 = B3 || H3 || p11, J2 = q3({ address: T6, classes: a4 != null ? a4 : {}, sender: (t5 == null ? void 0 : t5[0].sender) || T6, status: r3, toastId: s7, transactionDisplayInfo: o4 }), m8 = () => {
    i8 == null || i8(s7);
  };
  return (0, import_react.useEffect)(() => {
    if (!(!f4 || !n9 || e4.current)) return e4.current = setTimeout(() => {
      m8();
    }, n9), () => {
      e4.current && (m8(), clearTimeout(e4.current));
    };
  }, [n9, f4]), { isCrossShard: !d6, progress: q5, isPending: y3, isTimedOut: p11, toastDataState: J2, handleDeleteToast: m8 };
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-F6HZWORP.js
var import_dist3 = __toESM(require_dist());
var import_react2 = __toESM(require_react());
var import_classnames = __toESM(require_classnames());
v();
var p7 = ({ className: a4 = "dapp-transaction-toast-wrapper", children: i8, id: n9, styles: t5 }) => import_react2.default.createElement("div", { id: n9, className: (0, import_classnames.default)(t5 == null ? void 0 : t5.toasts, t5 == null ? void 0 : t5.toastWrapper, a4) }, i8);
var v4 = u3(p7, { ssrStyles: () => import("./transactionsToastList.styles-HYPOHPXZ.js"), clientStyles: () => (n3(), gt(r2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-TWAXQDQV.js
var import_dist7 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-NUS7PJPI.js
var import_dist4 = __toESM(require_dist());
var import_react3 = __toESM(require_react());
v();
T();
var D4 = ({ className: o4 = "dapp-default-toast-delete-button", onClick: s7 }) => import_react3.default.createElement("button", { type: "button", className: o4, onClick: s7, "data-testid": "deleteToastButton" }, import_react3.default.createElement(FontAwesomeIcon, { icon: faTimes, size: "xs" }));

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-JXC4YTUO.js
var import_dist6 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-CNNQRE7O.js
var import_dist5 = __toESM(require_dist());
var import_react4 = __toESM(require_react());
var import_classnames2 = __toESM(require_classnames());
v();
T();
var S2 = { icon: faCheck };
var e3 = { icon: faTimes };
var k4 = { icon: faCircleNotch };
var w = { pending: k4, success: S2, fail: e3, invalid: e3, timedOut: e3 };
var A = ({ className: I4, hash: n9, status: t5, iconSrc: r3, isTimedOut: l2, globalStyles: a4, styles: o4 }) => {
  var p11;
  let m8 = t5 ? w[t5] : null, c3 = (p11 = r3 == null ? void 0 : r3.icon) != null ? p11 : m8 == null ? void 0 : m8.icon;
  return import_react4.default.createElement("div", { className: (0, import_classnames2.default)(o4 == null ? void 0 : o4.container, I4), key: n9, "data-testid": "transactionDetailsToastBody" }, !l2 && c3 != null && import_react4.default.createElement(FontAwesomeIcon, { icon: c3, className: (0, import_classnames2.default)(o4 == null ? void 0 : o4.icon, { "fa-spin slow-spin": t5 === "pending" }) }), import_react4.default.createElement("span", { className: o4 == null ? void 0 : o4.trim }, import_react4.default.createElement(G, { text: n9 })), import_react4.default.createElement(D3, { text: n9 }), import_react4.default.createElement(H2, { page: `/${u}/${n9}`, className: a4 == null ? void 0 : a4.ml2 }));
};
var U = u3(A, { ssrStyles: () => import("./transactionDetails.styles-IZKFPIA3.js"), clientStyles: () => (i4(), gt(s3)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-JXC4YTUO.js
var import_react5 = __toESM(require_react());
v();
u2();
g();
var W2 = ({ title: c3, transactions: t5, isTimedOut: f4 = false, className: S4 = "dapp-transaction-details", styles: e4 }) => {
  if (t5 == null) return null;
  let { address: y3 } = s(), g5 = (0, import_react5.useMemo)(() => {
    let r3 = t5.filter((o4) => !h(W[o4 == null ? void 0 : o4.status])).length, s7 = t5.length;
    return s7 === 1 && r3 === 1 ? h(W[t5[0].status]) ? "Processing transaction" : "Transaction processed" : `${r3} / ${s7} transactions processed`;
  }, [t5]), D7 = t5.length === 1 && t5[0].sender !== y3;
  return import_react5.default.createElement(import_react5.default.Fragment, null, c3 && import_react5.default.createElement("div", { className: e4 == null ? void 0 : e4.title }, c3), !D7 && import_react5.default.createElement("div", { className: e4 == null ? void 0 : e4.status }, g5), t5.map(({ hash: r3, status: s7 }) => {
    let o4 = { className: S4, hash: r3, status: W[s7], isTimedOut: f4 };
    return import_react5.default.createElement(U, xt(yt({}, o4), { key: r3 }));
  }));
};
var F = u3(W2, { ssrStyles: () => import("./transactionDetails.styles-IZKFPIA3.js"), clientStyles: () => (i4(), gt(s3)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-TWAXQDQV.js
var import_react6 = __toESM(require_react());
var import_classnames3 = __toESM(require_classnames());
v();
T();
var g3 = ({ style: a4, toastDataState: n9, transactions: f4, showCloseButton: v6, onDeleteToast: N2, toastTitle: h3, isTimedOut: D7, customElements: t5 = { CustomCloseButton: D4, TransactionToastStatusIcon: FontAwesomeIcon, TransactionDetails: F }, globalStyles: i8, styles: s7 }) => {
  let r3 = t5 == null ? void 0 : t5.TransactionDetails, T6 = t5 == null ? void 0 : t5.TransactionToastStatusIcon, e4 = t5 == null ? void 0 : t5.CustomCloseButton;
  return import_react6.default.createElement("div", { className: a4.content, "data-testid": "transactionToastContent" }, import_react6.default.createElement("div", { className: a4.left }, import_react6.default.createElement("div", { className: (0, import_classnames3.default)(a4.icon, n9.iconClassName) }, T6 && import_react6.default.createElement(T6, { size: "5x", icon: n9.icon, className: a4.svg }))), import_react6.default.createElement("div", { className: a4.right }, import_react6.default.createElement("div", { className: a4.heading }, import_react6.default.createElement("h5", { className: (0, import_classnames3.default)([i8 == null ? void 0 : i8.h5, a4.mb4]), "data-testid": "transactionToastTitle" }, n9.title), v6 && e4 && import_react6.default.createElement(e4, { className: s7 == null ? void 0 : s7.close, onClick: N2 })), import_react6.default.createElement("div", { className: a4.footer }, r3 && import_react6.default.createElement(r3, { transactions: f4, title: h3, isTimedOut: D7 }))));
};
var J = u3(g3, { ssrStyles: () => import("./transactionToast.styles-7XAHICMD.js"), clientStyles: () => (c2(), gt(s2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-6BJESN7L.js
var import_dist8 = __toESM(require_dist());
var import_react7 = __toESM(require_react());
var import_classnames4 = __toESM(require_classnames());
v();
p();
e();
var m3 = "toastProgress";
var L = ({ id: a4, children: S4, progress: s7, done: R3, expiresIn: N2 = 10 * 60, className: E2 = "dapp-progress", isCrossShard: P3 = false, styles: o4 }) => {
  let { currentRemaining: W3, totalSeconds: b } = (0, import_react7.useMemo)(() => {
    let r3 = (f.session.getItem(m3) || {})[a4], t5 = s7 ? s7.endTime - s7.startTime : 0;
    return { currentRemaining: r3 || 100, totalSeconds: t5 };
  }, []), i8 = (0, import_react7.useRef)(null), l2 = (0, import_react7.useRef)(), n9 = (0, import_react7.useRef)(W3);
  (0, import_react7.useEffect)(() => {
    if (s7 != null) return R3 ? A2() : O3(), () => {
      clearInterval(l2.current);
    };
  }, [s7, R3]);
  function _() {
    let e4 = f.session.getItem(m3);
    !Boolean(e4 == null ? void 0 : e4[a4]) || (delete e4[a4], v6(e4));
  }
  function g5(e4) {
    let r3 = f.session.getItem(m3) || {};
    r3[a4] = e4, v6(r3);
  }
  function v6(e4) {
    f.session.setItem({ key: m3, data: e4, expires: r(N2) });
  }
  function A2() {
    if (n9.current < 100) {
      c3(0);
      return;
    }
    l2.current = setInterval(() => {
      if (i8.current == null) return;
      let r3 = n9.current - 1;
      r3 <= 0 ? (clearInterval(l2.current), _(), c3(0)) : (g5(r3), c3(r3));
    }, i3);
  }
  function O3() {
    if ((f.session.getItem(m3) || {})[a4] === n9.current) {
      let t5 = D2({ remaining: n9.current, totalSeconds: b, isCrossShard: P3 });
      c3(t5), g5(t5);
    }
    l2.current = setInterval(() => {
      if (i8.current == null) return;
      let t5 = D2({ remaining: n9.current, totalSeconds: b, isCrossShard: P3 });
      g5(t5), c3(t5);
    }, i3);
  }
  function c3(e4) {
    n9.current = e4, w2();
  }
  function w2() {
    if (i8.current == null) return;
    let e4 = n9.current;
    i8.current.style.width = `${e4}%`, i8.current.ariaValueNow = e4;
  }
  return s7 ? import_react7.default.createElement("div", { className: (0, import_classnames4.default)(o4 == null ? void 0 : o4.progress, E2) }, import_react7.default.createElement("div", { ref: i8, role: "progressbar", "aria-valuemin": 0, "aria-valuemax": 100, className: o4 == null ? void 0 : o4.bar }), S4) : import_react7.default.createElement(import_react7.default.Fragment, null, S4);
};
var X = u3(L, { ssrStyles: () => import("./progressStyles-VC5JXML3.js"), clientStyles: () => (n4(), gt(s4)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-7PSZY4BT.js
var import_react8 = __toESM(require_react());
v();
var R2 = ({ className: f4 = "dapp-transaction-toast", customization: t5, endTimeProgress: y3, lifetimeAfterSuccess: a4, onDelete: c3, startTimestamp: g5, status: S4, styles: r3, title: D7 = "", toastId: o4, transactions: e4 }) => {
  var p11, i8;
  let { isCrossShard: W3, progress: x3, isPending: n9, isTimedOut: T6, toastDataState: N2, handleDeleteToast: w2 } = St({ toastId: o4, transactions: e4, status: S4, lifetimeAfterSuccess: a4, startTimestamp: g5, endTimeProgress: y3, onDelete: c3 }), E2 = (p11 = t5 == null ? void 0 : t5.Progress) != null ? p11 : X, O3 = (i8 = t5 == null ? void 0 : t5.TransactionToastContent) != null ? i8 : J;
  return import_react8.default.createElement(v4, { className: f4, id: `toast-${o4}` }, import_react8.default.createElement(E2, { key: o4, id: o4, progress: x3, expiresIn: a4, done: !n9 || T6, isCrossShard: W3 }, import_react8.default.createElement(O3, { customElements: t5 == null ? void 0 : t5.TransactionToastContentCustomElements, isTimedOut: T6, onDeleteToast: w2, showCloseButton: !n9, style: r3 != null ? r3 : {}, toastDataState: N2, toastTitle: D7, transactions: e4 != null ? e4 : [] })));
};
var L2 = u3(R2, { ssrStyles: () => import("./transactionToast.styles-7XAHICMD.js"), clientStyles: () => (c2(), gt(s2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-RWU2GT72.js
var import_react9 = __toESM(require_react());
v();
var S3 = ({ className: e4, signedTransactionsToRender: r3, successfulToastLifetime: i8, handleDeleteTransactionToast: o4, startTimestamp: T6, toastId: t5 }) => {
  let s7 = r3[t5];
  if (s7 == null || (s7 == null ? void 0 : s7.transactions) == null || (s7 == null ? void 0 : s7.status) == null) return null;
  let { transactions: c3, status: l2 } = s7;
  return import_react9.default.createElement(L2, { key: t5, startTimestamp: T6, toastId: t5, transactions: c3, status: l2, lifetimeAfterSuccess: i8, onDelete: o4, className: e4 });
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-RWOA5CAH.js
var import_dist22 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-ZEPKB7OE.js
var import_dist12 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-4PHVTQ26.js
var import_dist11 = __toESM(require_dist());
var import_react10 = __toESM(require_react());
v();
var P = ({ CustomCloseButton: t5, onDelete: e4 }) => {
  let { styles: o4 } = v3({ ssrImportCallback: () => import("./customToast.styles-EO5DPZLX.js"), clientImportCallback: () => (n5(), gt(s5)).default });
  return (0, import_react10.useMemo)(() => t5 ? import_react10.default.createElement(t5, { onClick: e4 }) : import_react10.default.createElement(D4, { onClick: e4, className: o4 == null ? void 0 : o4.close }), [t5, e4, o4]);
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-ZEPKB7OE.js
var import_react11 = __toESM(require_react());
var import_classnames5 = __toESM(require_classnames());
v();
var C2 = ({ onDelete: T6, message: n9, messageComponent: s7, CustomCloseButton: c3, className: d6 = "dapp-custom-toast", toastId: f4, styles: o4 }) => {
  let l2 = P({ onDelete: T6, CustomCloseButton: c3 });
  return import_react11.default.createElement("div", { id: f4, className: (0, import_classnames5.default)(o4 == null ? void 0 : o4.toasts, o4 == null ? void 0 : o4.toastWrapper, d6) }, import_react11.default.createElement(import_react11.default.Fragment, null, l2, s7 || n9));
};
var x = u3(C2, { ssrStyles: () => import("./transactionsToastList.styles-HYPOHPXZ.js"), clientStyles: () => (n3(), gt(r2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-ADMYODWA.js
var import_dist19 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-F3MQEZD4.js
var import_dist18 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-CIFVVBRB.js
var import_dist14 = __toESM(require_dist());

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-PDQPLX7X.js
var import_dist13 = __toESM(require_dist());
var import_react12 = __toESM(require_react());
v();
var n7 = ({ children: e4, styles: t5 }) => import_react12.default.createElement("div", { className: t5 == null ? void 0 : t5.footer }, e4);
var T4 = u3(n7, { ssrStyles: () => import("./transactionToast.styles-7XAHICMD.js"), clientStyles: () => (c2(), gt(s2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-CIFVVBRB.js
var import_react13 = __toESM(require_react());
v();
var d3 = (o4) => {
  let t5 = I(o4.toastId);
  return import_react13.default.createElement(T4, yt({}, o4), t5 && import_react13.default.createElement(t5, null));
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-HJD3JB4Z.js
var import_dist15 = __toESM(require_dist());
var import_react14 = __toESM(require_react());
var import_classnames6 = __toESM(require_classnames());
v();
var d4 = (s7) => {
  var e4, r3;
  let { className: l2, message: n9, status: o4 } = s7;
  return import_react14.default.createElement(T4, null, o4 && import_react14.default.createElement("div", { className: (e4 = s7.styles) == null ? void 0 : e4.status }, o4), import_react14.default.createElement("div", { className: (0, import_classnames6.default)((r3 = s7.styles) == null ? void 0 : r3.container, l2) }, n9));
};
var M = u3(d4, { ssrStyles: () => import("./transactionDetails.styles-IZKFPIA3.js"), clientStyles: () => (i4(), gt(s3)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-DXUIEUQC.js
var import_dist16 = __toESM(require_dist());
var import_react15 = __toESM(require_react());
v();
d2();
var v5 = (s7) => {
  var e4;
  let { className: u5, transaction: f4 } = s7, { address: o4 } = s(), { network: { explorerAddress: y3 } } = p5(), r3 = q2({ transaction: f4, address: o4 != null ? o4 : "", explorerAddress: y3 });
  return import_react15.default.createElement(T4, null, import_react15.default.createElement("div", { className: (e4 = s7.styles) == null ? void 0 : e4.status }, import_react15.default.createElement(Z, { transaction: r3 })), import_react15.default.createElement(U, { className: u5, hash: r3.txHash, status: r3.status }));
};
var q4 = u3(v5, { ssrStyles: () => import("./transactionDetails.styles-IZKFPIA3.js"), clientStyles: () => (i4(), gt(s3)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-FV5YN5KV.js
var import_dist17 = __toESM(require_dist());
v();
m();
function m5(i8) {
  var a4;
  try {
    let n9 = i8, s7 = D(xt(yt({}, n9), { chainID: (a4 = n9.chainID) != null ? a4 : "D" }));
    return Boolean(s7);
  } catch (n9) {
    return false;
  }
}

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-F3MQEZD4.js
var import_react16 = __toESM(require_react());
v();
var P2 = (o4) => {
  let { transaction: r3, message: c3, component: f4 } = o4;
  return r3 && m5(r3) ? import_react16.default.createElement(q4, yt({}, o4)) : c3 ? import_react16.default.createElement(M, yt({}, o4)) : f4 !== void 0 ? import_react16.default.createElement(d3, yt({}, o4)) : import_react16.default.createElement(T4, null);
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-ADMYODWA.js
var import_react17 = __toESM(require_react());
var import_classnames7 = __toESM(require_classnames());
v();
var g4 = (t5) => {
  let { className: f4 = "dapp-custom-toast", onDelete: T6, icon: r3 = faInfo, iconClassName: v6, title: N2 = "", toastId: I4, CustomCloseButton: u5, globalStyles: i8, styles: o4 } = t5, h3 = P({ onDelete: T6, CustomCloseButton: u5 });
  return import_react17.default.createElement(v4, { className: f4, id: I4 }, import_react17.default.createElement("div", { className: o4 == null ? void 0 : o4.content }, import_react17.default.createElement("div", { className: o4 == null ? void 0 : o4.left }, import_react17.default.createElement("div", { className: (0, import_classnames7.default)(o4 == null ? void 0 : o4.icon, v6) }, r3 && import_react17.default.createElement(FontAwesomeIcon, { size: "5x", icon: r3, className: o4 == null ? void 0 : o4.svg }))), import_react17.default.createElement("div", { className: o4 == null ? void 0 : o4.right }, import_react17.default.createElement("div", { className: o4 == null ? void 0 : o4.heading }, import_react17.default.createElement("h5", { className: (0, import_classnames7.default)([i8 == null ? void 0 : i8.h5, o4 == null ? void 0 : o4.mb4]) }, N2), h3), import_react17.default.createElement(P2, yt({}, t5)))));
};
var E = u3(g4, { ssrStyles: () => import("./transactionToast.styles-7XAHICMD.js"), clientStyles: () => (c2(), gt(s2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-H5BRPXWX.js
var import_dist20 = __toESM(require_dist());
var import_react18 = __toESM(require_react());
var import_classnames8 = __toESM(require_classnames());
v();
var a2 = ({ onDelete: u5, message: e4, component: s7, CustomCloseButton: C3, className: T6 = "dapp-custom-toast", toastId: c3, styles: o4 }) => {
  let d6 = P({ onDelete: u5, CustomCloseButton: C3 });
  return import_react18.default.createElement("div", { id: c3, className: (0, import_classnames8.default)(o4 == null ? void 0 : o4.toasts, o4 == null ? void 0 : o4.toastWrapper, T6) }, import_react18.default.createElement(import_react18.default.Fragment, null, d6, s7 ? s7() : e4));
};
var x2 = u3(a2, { ssrStyles: () => import("./transactionsToastList.styles-HYPOHPXZ.js"), clientStyles: () => (n3(), gt(r2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-ANT3P3HL.js
var import_dist21 = __toESM(require_dist());
var import_react19 = __toESM(require_react());
v();
var i7 = ({ duration: e4, onDelete: s7 }) => {
  (0, import_react19.useEffect)(() => {
    let o4;
    return e4 && (o4 = setTimeout(s7, e4)), () => {
      clearTimeout(o4);
    };
  }, [e4]);
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-RWOA5CAH.js
var import_react20 = __toESM(require_react());
v();
u2();
var D6 = (t5) => {
  let { duration: f4, onDelete: h3 } = t5;
  if (i7({ duration: f4, onDelete: h3 }), t5.component) return import_react20.default.createElement(x2, yt({}, t5));
  if (t5.transaction) {
    let y3 = t5.transaction, s7 = t5.transaction, C3 = y3.txHash || s7.hash;
    return import_react20.default.createElement(L2, xt(yt({}, t5), { status: O[t5.transaction.status], transactions: [xt(yt({}, s7), { hash: C3 })] }));
  }
  return t5.icon ? import_react20.default.createElement(E, yt({}, t5)) : import_react20.default.createElement(x, yt({}, t5));
};

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-J5BVHYAZ.js
var import_react21 = __toESM(require_react());
var import_classnames9 = __toESM(require_classnames());
var import_react_dom = __toESM(require_react_dom());
v();
c();
g2();
l();
u2();
g();
var Tt = ({ className: H3 = "transactions-toast-list", transactionToastClassName: S4, customToastClassName: M2, signedTransactions: $2, successfulToastLifetime: y3, parentElement: q5, children: z, styles: i8 }) => {
  let [U2, j3] = (0, import_react21.useState)(false), A2 = k3(p3), r3 = k3(i2), c3 = d(), { signedTransactions: J2 } = p4(), n9 = $2 || J2, h3 = (0, import_react21.useRef)(n9);
  (0, import_react21.useEffect)(() => {
    h3.current = n9;
  }, [n9]);
  let K = (t5) => {
    H(t5);
  }, T6 = (t5) => {
    c3(k(t5)), h2(t5);
  }, I4 = (0, import_react21.useCallback)(() => {
    var t5;
    for (let o4 in n9) {
      let s7 = n9[o4];
      if ((t5 = s7 == null ? void 0 : s7.customTransactionInformation) == null ? void 0 : t5.signWithoutSending) continue;
      r3.some((d6) => `${d6.toastId}` == `${o4}`) || c3(j(o4));
    }
  }, [c3, n9, r3]);
  (0, import_react21.useEffect)(() => {
    I4();
  }, [n9, I4]);
  let O3 = (0, import_react21.useMemo)(() => r3.map(({ toastId: t5, type: o4, startTimestamp: s7 }, e4) => import_react21.default.createElement(S3, xt(yt({}, { signedTransactionsToRender: n9, toastId: t5, type: o4, startTimestamp: s7, successfulToastLifetime: y3, handleDeleteTransactionToast: T6, className: S4 }), { key: s7 + e4 }))), [r3, n9, y3, T6, S4]), Q = A2.map((t5) => {
    var e4;
    let o4 = (e4 = I(t5.toastId)) != null ? e4 : null, s7 = v2(t5.toastId);
    return import_react21.default.createElement(D6, xt(yt({ key: t5.toastId }, t5), { component: o4, onDelete: () => {
      K(t5.toastId), s7 == null || s7();
    }, className: M2 }));
  }), C3 = () => {
    i2(a.getState()).forEach((o4) => {
      let s7 = h3.current[o4.toastId];
      if (!s7) return;
      let { status: e4 } = s7, m8 = j2(e4), d6 = e4 === "signed";
      !m8 && !d6 && T6(o4.toastId);
    });
  };
  return (0, import_react21.useEffect)(() => (j3(true), window == null || window.addEventListener("beforeunload", C3), () => {
    window == null || window.removeEventListener("beforeunload", C3);
  }), []), import_react21.default.createElement(import_react21.default.Fragment, null, U2 && (0, import_react_dom.createPortal)(import_react21.default.createElement("div", { className: (0, import_classnames9.default)(i8 == null ? void 0 : i8.toasts, H3) }, Q, O3, z), q5 || (document == null ? void 0 : document.body)));
};
var Bt = u3(Tt, { ssrStyles: () => import("./transactionsToastList.styles-HYPOHPXZ.js"), clientStyles: () => (n3(), gt(r2)).default });

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-2NMQCGC7.js
var import_dist24 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-2YCTW6TI.js
var import_dist25 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-EDSKSLOU.js
var import_dist26 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-CEQ5FXRO.js
var import_dist27 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-WAY2DWBE.js
var import_dist28 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-6ZVGZMVA.js
var import_dist29 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-JQMYW5MI.js
var import_dist30 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-YF4C2R42.js
var import_dist31 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-3ETS27SD.js
var import_dist32 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-PNVRKZHJ.js
var import_dist33 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-DNUCX5HA.js
var import_dist34 = __toESM(require_dist());
v();

// node_modules/@multiversx/sdk-dapp/__chunks__/chunk-RH7KUPWQ.js
var import_dist35 = __toESM(require_dist());
v();
export {
  Bt as TransactionsToastList,
  Tt as TransactionsToastListComponent
};
//# sourceMappingURL=@multiversx_sdk-dapp_UI_TransactionsToastList_TransactionsToastList.js.map

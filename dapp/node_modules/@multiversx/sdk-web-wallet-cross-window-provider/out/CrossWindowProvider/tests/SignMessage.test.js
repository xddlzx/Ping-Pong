"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const test_utils_1 = require("../../test-utils");
const CrossWindowProvider_1 = require("../CrossWindowProvider");
const out_1 = require("@multiversx/sdk-core/out");
describe('CrossWindowProvider Login', () => {
    let crossWindowProvider;
    let walletWindowMock;
    let windowOpenSpy;
    beforeEach(() => {
        walletWindowMock = (0, test_utils_1.getWalletWindowMock)();
        CrossWindowProvider_1.CrossWindowProvider.getInstance().getWindowManager().postMessage = jest
            .fn()
            .mockImplementation(() => ({
            payload: {
                data: {
                    status: 'signed',
                    signature: Buffer.from('testSignature').toString('hex')
                }
            }
        }));
        crossWindowProvider = CrossWindowProvider_1.CrossWindowProvider.getInstance();
        windowOpenSpy = jest.spyOn(window, 'open');
        windowOpenSpy.mockImplementation(() => walletWindowMock);
    });
    it('should sign a message correctly', () => __awaiter(void 0, void 0, void 0, function* () {
        var _a;
        const mockMessage = new out_1.Message({ data: Buffer.from('test') });
        crossWindowProvider.setAddress('erd1qyu5wthldzr8wx5c9ucg8kjagg0jfs53s8nr3zpz3hypefsdd8ssycr6th');
        yield crossWindowProvider.init();
        const result = yield crossWindowProvider.signMessage(mockMessage);
        expect((_a = result.signature) === null || _a === void 0 ? void 0 : _a.toString()).toBe('testSignature');
    }));
});
//# sourceMappingURL=SignMessage.test.js.map